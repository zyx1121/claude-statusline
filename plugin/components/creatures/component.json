{
  "$schema": "statusline/component@1",
  "id": "creatures",
  "name": "Creatures",
  "version": "0.5.0",
  "author": {
    "github": "zyx1121"
  },
  "description": "A tiny Seer-creature world that paces across the status line (pure local animation, one tick per refresh).",
  "i18n": {
    "zh-Hant": {
      "name": "寶可怪",
      "description": "在 status line 上踱步的 Seer 寶可怪微型世界（純本機動畫，每次 refresh 一個 tick）。"
    },
    "zh-Hans": {
      "name": "宝可怪",
      "description": "在 status line 上踱步的 Seer 宝可怪微型世界（纯本机动画，每次 refresh 一个 tick）。"
    }
  },
  "type": "line",
  "runtime": "python3",
  "render": {
    "entry": "render.py",
    "ttl": 1
  },
  "inputs": [
    "session_id"
  ],
  "requires": {
    "bin": [
      "python3"
    ]
  },
  "config": {
    "schema": {
      "ground": {
        "type": "string",
        "default": "grass",
        "desc": "Ground strip style under the creatures (\"grass\" paints a solid green grass band; anything else = bare)."
      },
      "resident": {
        "type": "string",
        "default": "132",
        "desc": "Comma-separated dex numbers of permanent residents that never expire (e.g. \"132,54\" = Ditto + Psyduck). Default 132 (Ditto)."
      },
      "blocks": {
        "type": "string",
        "default": "octant",
        "desc": "Sub-pixel block style. \"octant\" (default) draws each cell's full 2×4 sub-pixel grid with Unicode 16 octant glyphs — needs a terminal that custom-draws them (Ghostty, kitty, WezTerm, recent iTerm2). \"quadrant\" folds to 2×2 U+2580–259F blocks that render in any monospace font (e.g. Terminal.app), at half the vertical detail."
      }
    }
  },
  "placement": {
    "slot": "middle",
    "order": 10
  },
  "capabilities": {
    "network": [],
    "exec": false,
    "fs_write": [
      "$STATE_DIR"
    ],
    "stdin_fields": []
  }
}
